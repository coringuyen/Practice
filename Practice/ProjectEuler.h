#pragma once
#include <string>
#include <sstream>

class PE
{
public:
	int sumSquareDifference(int first, int second);
	int sumOfSquare(int num);
	int squareOfSum(int num);

	int powerDigitSum(int power);

	int smallestMultiple(int num);

	int sumOfMultiple3n5(int num);

	int numberLetterCounts(std::string numbers);
};
